function addNumbers(num1, num2) {
  return n1 + n2;
}

function handleText(inputA, inputB) {
  if (inputA === "test" && inputB !== "temp") {
    return inputB;
  }
  return `${inputB} random text`;
}

let numOfCats = 0;
